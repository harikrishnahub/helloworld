# include <stdio.h>
# include <stdlib.h>
# include <stdio_ext.h>

# define ISDIGIT(c)	(c >= '0' && c <= '9') ? 1 :0
# define INVALID	0
int Isalnum ( int );
int Isalpha ( int );
int Isdigit ( int );
int Isprint ( int );
int Isupper ( int );
int Isxdigit( int );
int Islower ( int );
int Iscntrl ( int );
int Strlen  ( char * );
int getint  ( );

